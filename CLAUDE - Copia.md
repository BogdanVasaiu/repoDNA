# CLAUDE.md
> Auto-generated by repoDNA · Smart Update · Model: `qwen2.5-coder:7b`
> Generated: 2026-05-21T11:49:13.965Z

---

## Project Overview

 repoDNA is a five-step wizard that runs in your browser.

**01 — Ollama setup**
Checks whether Ollama is running and lists all locally installed models. If Ollama is not installed, the interface shows the exact install command for your platform (Windows, macOS, Linux). A curated model reference table is also available here, updated regularly, with models ranked by quality, speed, and VRAM requirements — so you can pick and pull the right one before starting.

**02 — Add your projects**
Register one or more local project paths with an optional description. The description is passed to the AI as context before any file is read.

**03 — Select what to scan**
An intelligent classifier examines your project structure — detects Node.js, Python, Rust, Go, and more — and auto-excludes folders like `node_modules`, build output, and binary files. Every decision is visible and overridable per file or per folder. Custom rules are saved per project.

**04 — Configure the analysis**
Select the Ollama model from a dropdown that lists everything currently installed in your Ollama instance. Set the analysis depth (see [precision modes](#analysis-precision) below). Advanced options include:

- **Smart Update** — only re-analyzes files that changed since the last run
- **File tree** — optionally embeds an ASCII tree of the project in the output (included files only, or full project)

**05 — Run**
A live dashboard shows each file being processed, its markdown result appearing in real time, and a running activity log. Failed files can be retried individually. Each result card can also be edited directly in the UI — changes are saved to the cache and the output file is rebuilt immediately from the cache, not from the project. This keeps manual edits safe: the algorithm never overwrites them by re-reading the project files.

---

## File Tree

<pre class="file-tree-block">├── .claude/
│   ├── launch.json
│   └── settings.local.json
├── docs/
│   ├── shared/
│   │   ├── assets/
│   │   │   ├── agents/
│   │   │   │   ├── Amp-icon.svg
│   │   │   │   ├── Claude-icon.svg
│   │   │   │   ├── Cline-icon.svg
│   │   │   │   ├── Codex-icon.svg
│   │   │   │   ├── Copilot-icon.svg
│   │   │   │   ├── Cursor-icon.svg
│   │   │   │   ├── Gemini-icon.svg
│   │   │   │   ├── Generic-icon.svg
│   │   │   │   ├── Openclaw-icon.svg
│   │   │   │   ├── Opencode-icon.svg
│   │   │   │   ├── RooCode-icon.svg
│   │   │   │   └── Windsurf-icon.svg
│   │   │   ├── steps/
│   │   │   │   ├── Screenshot 2026-05-14 102440.png
│   │   │   │   ├── step-01.png
│   │   │   │   ├── step-02.png
│   │   │   │   ├── step-03.png
│   │   │   │   ├── step-04.png
│   │   │   │   └── step-05.png
│   │   │   ├── nodejs-icon.svg
│   │   │   └── ollama-icon.svg
│   │   ├── animation1-standalone.html
│   │   ├── dna.js
│   │   └── tokens.css
│   ├── CNAME
│   ├── index.html
│   └── landing.css
├── public/
│   ├── shared/
│   │   ├── assets/
│   │   │   ├── agents/
│   │   │   │   ├── Amp-icon.svg
│   │   │   │   ├── Claude-icon.svg
│   │   │   │   ├── Cline-icon.svg
│   │   │   │   ├── Codex-icon.svg
│   │   │   │   ├── Copilot-icon.svg
│   │   │   │   ├── Cursor-icon.svg
│   │   │   │   ├── Gemini-icon.svg
│   │   │   │   ├── Generic-icon.svg
│   │   │   │   ├── Openclaw-icon.svg
│   │   │   │   ├── Opencode-icon.svg
│   │   │   │   ├── RooCode-icon.svg
│   │   │   │   └── Windsurf-icon.svg
│   │   │   ├── nodejs-icon.svg
│   │   │   └── ollama-icon.svg
│   │   ├── animation1-standalone.html
│   │   ├── dna.js
│   │   └── tokens.css
│   ├── app.mjs
│   ├── index.html
│   └── styles.css
├── builder.mjs
├── classifier.mjs
├── CLAUDE.md
├── config.mjs
├── debug.mjs
├── LICENSE
├── main.mjs
├── ollama_models.json
├── ollama.mjs
├── package.json
├── README.md
├── scanner.mjs
├── server.mjs
├── uninstall.mjs
└── update.mjs</pre>

---

## Dependency Graph

> Auto-extracted from static import analysis.

**`server.mjs`** imports: `./config.mjs`, `./scanner.mjs`, `./ollama.mjs`, `./builder.mjs`, `./classifier.mjs`
**`scanner.mjs`** imports: `./classifier.mjs`

### Most imported modules
- `./classifier.mjs` ← used by 2 files
- `./config.mjs` ← used by 1 file
- `./scanner.mjs` ← used by 1 file
- `./ollama.mjs` ← used by 1 file
- `./builder.mjs` ← used by 1 file

---
 
## Other Files

### `README.md`
**Role:** Provides instructions on how to use repoDNA, a tool that generates AI context files from any codebase locally.

**Key exports:** none

**Non-obvious deps:**  
- `node` (Node.js runtime)
- `ollama-cli` (Ollama command-line interface)

**Watch out:** None

---

### `scanner.mjs`
**Role:** This file is responsible for scanning and analyzing project files to detect their structure, categorize them based on language types like Node.js or Python, apply custom rules, exclude unnecessary folders/files, and build an ASCII tree representation of the project.

**Key exports:**
- **scanProject**: Scans a given root path for projects by walking through directories, classifying nodes, applying user-defined and global rules, and computing final statuses.
  - Returns:
    ```json
    {
      "tree": [...],
      "nodes": [...],
      "projectType": "...",
      "fileCount": ...,
      "timedOut": ...
    }
    ```
  
- **scanProjectExtensions**: Analyzes file extensions in a directory to count occurrences while excluding common excluded folder names like `node_modules`.
  - Returns:
    ```json
    [
        { "ext": ".js", "count": 10 },
        { "ext": ".py", "count": 5 },
        // other extension counts...
    ]
    ```

**Non-obvious deps:** none

**Watch out:** 
none

---

### `server.mjs`
**Role:** Manages the HTTP server for repoDNA's web interface, handling API requests and serving static files.
**Key exports:** none  
**Non-obvious deps:** fetch (for GitHub model refresh), child_process (execSync) — version-sensitive libraries. env vars consumed include REPODNA_DEBUG. global singletons: sseClients, appState. external services: GitHub for models update check.  
**Watch out:** Global state mutations in debugMode() function; ordering constraints on file operations during analysis runs; shape-sensitive objects like catResultMaps which require careful management to avoid duplicate entries. Side effects occur when writing logs or updating project configurations via APIs.

---

### `uninstall.mjs`
**Role:** This file is responsible for uninstalling repoDNA by removing its data directory and ensuring that the server, if running on port 3741, is stopped before proceeding with deletion.

**Key exports:**
- none

**Non-obvious deps:**
- fetch — used to check if a local HTTP server (repoDNA) is currently running.
- AbortController & setTimeout — manage asynchronous operations during checks.
- fs.rmSync — synchronously removes files or directories recursively from disk. 

**Watch out:** 
- The script assumes there's no global state mutation outside of what it explicitly manages within this function scope.
- It requires careful handling when run without `--yes` flag as it will prompt for confirmation and can permanently delete data in the user’s home directory (~/.repodna).

---

### `update.mjs`
**Role:** Manages the update process for repoDNA, checking if a newer version is available on GitHub and applying it.
  
**Key exports:** none

**Non-obvious deps:** 
- fetch — used to interact with GitHub API
- child_process.execSync — runs shell commands synchronously
  
**Watch out:** None. The script handles potential errors gracefully by providing clear messages and instructions without causing side effects or global state mutations when run in isolation. It also ensures that local changes are stashed before updating unless explicitly forced otherwise.

---
*Generated by [repoDNA](https://github.com/BogdanVasaiu/repodna)*
