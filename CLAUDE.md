# CLAUDE.md
> Auto-generated by repoDNA · Precision: **Adaptive** · Model: `qwen3-coder:30b`
> Generated: 2026-06-01T07:34:46.467Z

---

## Project Overview

 Every time you start a new session with your AI coding assistant, it starts from zero — no memory of your project, its structure, or its conventions. You spend the first minutes of every session re-explaining the same things.

repoDNA fixes this. It scans your codebase with an [Ollama](https://ollama.com) model and generates a structured context file your AI assistant reads at the start of every session — so it already understands your project before you write a single prompt. **No proprietary cloud API. No API keys. No accounts.**

Works with both local and cloud-hosted Ollama models — so you're not blocked if you don't have the hardware to run models yourself.

**No npm install. No build step.**  
The only thing you need to start is [Node.js](https://nodejs.org). Run one command and the browser UI opens — it guides you through Ollama setup, model selection, and everything else from there.

---

## File Tree

```
├── docs/
│   ├── shared/
│   │   ├── assets/
│   │   │   ├── agents/
│   │   │   │   ├── Amp-icon.svg
│   │   │   │   ├── Claude-icon.svg
│   │   │   │   ├── Cline-icon.svg
│   │   │   │   ├── Codex-icon.svg
│   │   │   │   ├── Copilot-icon.svg
│   │   │   │   ├── Cursor-icon.svg
│   │   │   │   ├── Gemini-icon.svg
│   │   │   │   ├── Generic-icon.svg
│   │   │   │   ├── Openclaw-icon.svg
│   │   │   │   ├── Opencode-icon.svg
│   │   │   │   ├── RooCode-icon.svg
│   │   │   │   └── Windsurf-icon.svg
│   │   │   ├── steps/
│   │   │   │   ├── step-01.webp
│   │   │   │   ├── step-02.webp
│   │   │   │   ├── step-03.webp
│   │   │   │   ├── step-04.webp
│   │   │   │   ├── step-05-1.webp
│   │   │   │   └── step-05-2.webp
│   │   │   ├── nodejs-icon.svg
│   │   │   └── ollama-icon.svg
│   │   ├── animation1-standalone.html
│   │   ├── dna.js
│   │   └── tokens.css
│   ├── CNAME
│   ├── index.html
│   └── landing.css
├── public/
│   ├── app.mjs
│   ├── index.html
│   └── styles.css
├── src/
│   ├── builder.mjs
│   ├── cache.mjs
│   ├── classifier.mjs
│   ├── config.mjs
│   ├── debug.mjs
│   ├── migrations.mjs
│   ├── ollama.mjs
│   ├── scanner.mjs
│   ├── server.mjs
│   └── store.mjs
├── tests/
│   └── store.test.mjs
├── help.mjs
├── LICENSE
├── main.mjs
├── ollama_models.json
├── package.json
├── README.md
├── uninstall.mjs
└── update.mjs
```

---

## Dependency Graph

> Auto-extracted from static import analysis.

**`src/server.mjs`** imports: `./config.mjs`, `./scanner.mjs`, `./ollama.mjs`, `./builder.mjs`, `./classifier.mjs`, `./cache.mjs`, `./migrations.mjs`, `./store.mjs`
**`tests/store.test.mjs`** imports: `../src/cache.mjs`, `../src/migrations.mjs`, `../src/store.mjs`
**`main.mjs`** imports: `./src/server.mjs`, `./help.mjs`
**`src/builder.mjs`** imports: `./classifier.mjs`
**`src/ollama.mjs`** imports: `./debug.mjs`
**`src/scanner.mjs`** imports: `./classifier.mjs`
**`src/store.mjs`** imports: `./migrations.mjs`

### Most imported modules
- `./classifier.mjs` ← used by 3 files
- `./migrations.mjs` ← used by 2 files
- `./src/server.mjs` ← used by 1 file
- `./help.mjs` ← used by 1 file
- `./debug.mjs` ← used by 1 file
- `./config.mjs` ← used by 1 file
- `./scanner.mjs` ← used by 1 file
- `./ollama.mjs` ← used by 1 file

---
 
## Source Code

### `docs/shared/dna.js`
**Role:** This file provides SVG-based DNA helix animation and typewriter text effects for the repoDNA project's browser UI. It dynamically generates animated DNA strands and types out the project name "repoDNA" with visual styling.
**Key exports:** `makeDna` — creates an animated DNA helix in an SVG element using trigonometric calculations and SVG DOM manipulation; `typewriter` — animates the typing of "repoDNA" into a specified HTML element with color-coded segments and cursor effect
**Non-obvious deps:** none
**Watch out:** The `makeDna` function mutates the global SVG element by clearing and repopulating it on every animation frame, and relies on `requestAnimationFrame` for continuous rendering which may conflict with strict timing constraints in test environments. The `typewriter` function uses `setTimeout` and modifies DOM content directly, potentially causing issues if called multiple times before previous animations complete.

---

### `help.mjs`
**Role:** Provides command-line usage instructions and flag descriptions for the repoDNA CLI tools, including `main.mjs`, `update.mjs`, and `uninstall.mjs`.
**Exports:** none

---

### `main.mjs`
**Role:** Starts the repoDNA server and opens the browser UI, handling command-line arguments for debug mode, auto-opening, and help display.
**Exports:** none

---

### `public/app.mjs`
**Role:** Manages global application state, user preferences, and UI rendering for the repoDNA web interface. Handles file selection history, project settings, and the interactive file tree visualization during the context generation workflow.
**Key exports:** 
`md2html(t)` — converts markdown text to HTML using the marked library or fallback sanitization
`_getModelStats()` — returns cached model performance statistics
`_loadModelStats()` — fetches and loads model speed data from the server
`_saveModelStat(modelName, tokensPerSecond)` — updates and persists model performance metrics
`_fmtModelSize(bytes)` — formats file size in GB or MB
`_lookupCatalogEntry(modelName)` — finds model metadata from the curated Ollama catalog
`_getFileCounts()` — retrieves stored file count data from localStorage
`_setFileCount(path, count)` — saves file count for a project path in localStorage
`_clearFileCount(path)` — removes file count entry for a project path from localStorage
`_splitPath(p)` — splits a file path into base name and directory components
`withHistory(label, fn, opts)` — wraps mutations with undo/redo history tracking
`applyProjectSettings(proj)` — applies project-specific configuration to global state
`selectProject(path)` — loads and initializes a project, triggering UI updates
`populateModels()` — updates the model dropdown with available Ollama models
`buildModeGrid()` — renders the precision mode selection UI
`renderTree()` — builds and displays the file tree in the UI
`renderAdvContent()` — updates advanced filtering UI elements
`renderCategoryChips()` — renders category filter chips in the UI
`refreshFileCount()` — updates the file count display
`renderSummary()` — renders the summary statistics panel
`saveCurrentProjectSettings()` — persists current project settings to localStorage
`rescanWithRules()` — triggers a new file scan using current filtering rules
`renderTabs()` — updates tab navigation UI
`showResultsPanel()` — displays the results panel
`renderResults(cat)` — renders results for a specific category
`showPreviewPanel()` — displays the preview panel
`renderPreview()` — renders the preview content
`_fcOpen()` — opens the file context finder modal
`_fcRefresh(changedFid)` — updates a specific file row in the finder modal
`_awaitStartupMigration()` — waits for any pending cache schema migrations
`checkForUpdate()` — checks for application updates
`runChecks()` — performs initial setup and validation checks
`loadOllamaModelsCatalog()` — loads the curated Ollama models catalog
`loadProjectList()` — fetches and displays available projects
**Non-obvious deps:** 
`marked` — global markdown parser used in `md2html` function
`localStorage` — used for persisting file counts and project settings
`fetch` — used for API calls to `/api/model-stats`, `/api/load-config`, and `/api/project-list`
`S` — global state object that is mutated directly by many functions
`History` — global history manager that tracks user actions for undo/redo
**Side effects:** 
Modifies global `S` state object on every function call
Subscribes to `keydown` events for undo/redo shortcuts
Updates DOM elements directly via `document.getElementById` and `querySelector`
Writes to `localStorage` for file counts and project settings
Makes network requests to `/api/model-stats`, `/api/load-config`, and `/api/project-list`
**Architecture:** 
Imports no other project files directly; manages global state via `S` and `History`
Initializes and owns the global `S` state object and `History` instance
Exports functions that mutate global state and trigger UI updates
**Watch out:** 
The `History` object must be reset when switching projects to avoid cross-project mutations
`S.customRules` is replaced entirely during `applyProjectSettings` — not merged
`_fcRefresh` is a global function that can be called externally and must handle missing elements gracefully
`_modelStatsCache` is shared globally and updated via `_saveModelStat` which makes network requests
`S.userOverrides` is a Map that gets mutated directly by many functions and is used in history tracking

---

### `src/builder.mjs`
**Role:** [Manages static import parsing and dependency graph building for codebase analysis. Handles language-specific import resolution including TypeScript aliases, Go modules, Dart packages, and relative paths. Generates structured context for AI assistants by analyzing file relationships and categorizing project assets.]
**Key exports:** [categorize(rel) — maps file extensions to project categories using assignCategory from classifier.mjs]
**Non-obvious deps:** [none]
**Side effects:** [none]
**Architecture:** [Imports classifier.mjs for category assignment. Owns global state cache `_projectInfo` for project-level configuration like TypeScript aliases, Go modules, and Dart packages. Exports `buildClaudeMd` as stable public API for generating AI context files.]
**Watch out:** [File-specific gotchas: The `getProjectInfo` function caches project-level metadata; changes to project structure require a new run to refresh the cache. The `buildDependencyGraph` function requires `fileList` and `projectRoot` to be properly defined to avoid errors. The `buildClaudeMd` function expects `results` to contain valid category entries from `CATEGORY_META`.]

---

### `src/cache.mjs`
**Role:** Handles crash-safe read and write operations for cache files using atomic file system operations and backup restoration.
**Exports:** `recoverCacheFile`, `writeCache`, `readCache`

---

### `src/classifier.mjs`
**Role:** Defines the core logic for determining which files and directories to include or exclude when scanning a codebase. It manages project type detection, rule prioritization, and file classification based on naming, extension, and size heuristics.
**Key exports:** 
`detectProjectType(rootFiles)` — identifies project type from root files, returns one of: NODE_JS, PYTHON, RUST, JAVA_MAVEN, JAVA_GRADLE, GO, RUBY, PHP, DOTNET, UNREAL, FLUTTER, IOS, ANDROID, ELIXIR, C_CPP, SWIFT, UNITY, SCALA, GODOT, UNKNOWN
`computeEffectiveRules(customRules, globalExclusions, globalInclusions, projectType)` — merges default and project-specific rules into a unified set of exclusion/inclusion criteria for file classification
`classifyNode(node, projectType, rules)` — determines if a file or directory should be included, excluded, or marked as ambiguous based on its name, extension, size, and project context
`computeFinalStatus(node)` — resolves whether a node should be included or excluded based on user override or auto-status
`getDefaultRules()` — returns the full set of default exclusion and inclusion rules for use in UI or debugging
**Non-obvious deps:** none
**Side effects:** none
**Architecture:** Imports no other project files. Exports constants and functions that form the core classification logic. Owns the global rule sets and classification heuristics. The `classifyNode` function is the primary entry point for file status determination.
**Watch out:** The `detectProjectType` function relies on exact filename matches and may misidentify projects if root files are missing or renamed. The `classifyNode` function uses a strict priority order, so changes to rule precedence could alter classification results. The `computeEffectiveRules` function merges arrays without deduplication, which may cause unexpected behavior if duplicate entries are added.

---

### `src/config.mjs`
**Role:** Manages global and project-specific configuration data for repoDNA, including UI preferences, project lists, and per-project settings. Handles migration of legacy config formats and ensures consistent data storage in `~/.repodna` with dedicated project subdirectories.
**Key exports:** `loadConfig` — reads and returns the global configuration object, migrating legacy formats if needed. `saveConfig` — writes the provided configuration object to `~/.repodna/config.json`. `getProjectList` — returns the list of projects from the global config. `addProject` — adds or updates a project entry in the global config and ensures its data directory exists. `removeProject` — removes a project from the global config and deletes its associated cached data files. `updateProjectSettings` — updates settings for an existing project in the global config. `getProject` — retrieves a specific project entry by path from the global config. `hashCachePath`, `resultCachePath`, `newFilesCachePath` — return paths to cached data files for a given project within `~/.repodna/projects/<encoded>/`.
**Non-obvious deps:** none
**Watch out:** The `loadConfig` function mutates the default config object with saved values, which may cause unexpected behavior if defaults are modified elsewhere. The `addProject` function modifies the global config object directly and saves it, so it should not be called concurrently. The `removeProject` function deletes files and directories within `~/.repodna/projects/<encoded>/`, which could affect other processes using those files.

---

### `src/debug.mjs`
**Role:** This file manages the debugging state and logging for repoDNA, enabling or disabling debug mode and recording detailed file processing information to a log file.
**Exports:** `isDebugEnabled`, `enableDebug`, `disableDebug`, `clearDebugLog`, `debugLog`

---

### `src/migrations.mjs`
**Role:** Defines the schema versioning and migration logic for the repoDNA data store, managing compatibility between different versions of the global configuration and project caches.
**Exports:** `DATA_SCHEMA`, `MIGRATIONS`, `classifyChain`

---

### `src/ollama.mjs`
**Role:** Manages Ollama model interactions for codebase analysis, including adaptive precision modes, file content truncation strategies, prompt construction, and output validation for AI-generated context files.
**Key exports:** 
`checkOllama(host)` — verifies Ollama API availability and lists available models
`getEffectiveMode(precision, contentLength)` — selects appropriate analysis depth based on file size and user preference
`smartTruncate(content, maxChars)` — preserves import blocks while intelligently truncating large files
`buildPromptForTier(tier, rel, ext, truncated, projectDesc, modelName)` — constructs tailored prompts for different analysis depths
`detectAndFixLoop(text)` — identifies and removes repetitive output patterns from model responses
`sanitizeStructure(text)` — ensures consistent markdown formatting and prevents structural issues in generated content
`validateModelOutput(text, tier)` — checks that model output conforms to required template structure
`generateContextEntry(config, file, tier, projectDesc)` — orchestrates full AI context generation for a single file
**Non-obvious deps:** 
`LOG_FILE` from `./debug.mjs` — used for logging validation retries
`fetch` — global browser/node fetch API for HTTP requests to Ollama endpoints
`AbortSignal.timeout()` — used for enforcing 10-second timeouts on Ollama API calls
**Side effects:** 
Writes to `LOG_FILE` during validation retries when `REPODNA_DEBUG=1`
Makes network calls to configured Ollama host for model inference
**Architecture:** 
Imports `LOG_FILE` from `./debug.mjs` and uses it for logging
Exports `PRECISION_MODES` and `ADAPTIVE_THRESHOLDS` as global configuration constants
Exports `checkOllama`, `getEffectiveMode`, `smartTruncate`, `buildPromptForTier`, `detectAndFixLoop`, `sanitizeStructure`, `validateModelOutput`, and `generateContextEntry` as stable public APIs
**Watch out:** 
The `generateContextEntry` function assumes `config.model` and `config.ollamaHost` are properly initialized before calling
The `smartTruncate` function may produce truncated output if the import block consumes the entire character budget
The `validateModelOutput` function strictly enforces field presence and order, requiring exact template adherence for all tiers

---

### `src/scanner.mjs`
**Role:** Scans a project directory structure and builds a hierarchical tree of files and directories, determining their inclusion status based on project type and configured rules. It also computes extension usage statistics for the project.
**Key exports:** 
`scanProject` — scans the project root and returns a structured tree with file and directory nodes, including their classification and final status.
`scanProjectExtensions` — analyzes file extensions in the project and returns a sorted list of extensions with their usage counts.
**Non-obvious deps:** none
**Watch out:** The `buildTree` function mutates node objects in place when calculating directory status and sorting children, and relies on the order of node processing to correctly build parent-child relationships. The `scanProject` function uses global constants `MAX_DEPTH` and `MAX_FILES` that affect traversal behavior.

---

### `src/server.mjs`
**Role:** Manages the core HTTP server lifecycle, handles API endpoints for project configuration, file scanning, AI analysis orchestration, and real-time communication with the browser UI via Server-Sent Events (SSE). Coordinates the complete repoDNA workflow from startup through project analysis and output generation.
**Key exports:** 
`runAnalysis(config)` — executes the full project analysis pipeline using configured Ollama model and settings, managing file inclusion/exclusion, caching, retries, and result aggregation.
`checkOllama()` — verifies Ollama service availability and model readiness, returning status and available models.
`scanProject(projectPath, customRules, globalExclusions, globalInclusions)` — performs a recursive file tree scan of the project, applying custom and global filtering rules to determine which files to include in analysis.
`analyzeFileWithOllama(filePath, model, precision)` — sends a single file to Ollama for context analysis using specified model and precision mode, returning structured output.
`loadConfig()` — loads the persistent configuration file from disk, initializing default values if missing.
`saveConfig(config)` — writes updated configuration to disk, preserving all project settings and user preferences.
`getProjectList()` — retrieves list of all configured projects from the persistent store.
`addProject(projectPath, agentTarget, model, precision)` — registers a new project with specified settings, adding it to the configuration store.
`removeProject(projectPath)` — removes a project from the configuration store and deletes associated cache files.
`updateProjectSettings(projectPath, updates)` — modifies existing project settings in the configuration store.
`getProject(projectPath)` — fetches a specific project's configuration and metadata from the store.
`hashCachePath(projectPath)` — returns the absolute path to the hash cache file for a given project.
`resultCachePath(projectPath)` — returns the absolute path to the result cache file for a given project.
`newFilesCachePath(projectPath)` — returns the absolute path to the new files cache file for a given project.
`modelStatsCachePath(projectPath)` — returns the absolute path to the model stats cache file for a given project.
`buildClaudeMd(config)` — generates the final Claude-compatible context file based on analysis results.
`categorize(fileId, content)` — assigns a category to a file's content based on its structure and content.
`buildDependencyGraph()` — constructs a dependency graph of files within the project.
`getDefaultRules()` — returns default inclusion/exclusion rules for common project types.
`readCache(cachePath, defaultVal)` — reads a JSON cache file, returning default value if file doesn't exist or is invalid.
`writeCache(cachePath, data)` — writes data to a JSON cache file, creating parent directories if needed.
`recoverCacheFile(cachePath)` — attempts to recover a corrupted cache file by deleting it and returning default value.
`validateMigrationsChain()` — validates the integrity of the migration chain in `migrations.mjs`.
`migrateStore()` — performs data store migrations, clearing incompatible stores and updating schema versions.
**Non-obvious deps:** 
`process.env.REPODNA_DEBUG` — enables debug logging to `repodna-debug.log` when set to "1".
`process.env.OLLAMA_HOST` — overrides default Ollama host address if set.
`__dirname` — resolved to `src/` directory via `fileURLToPath(import.meta.url)` and used to locate `public/`, `docs/`, and `ollama_models.json`.
`DATA_SCHEMA` — imported from `migrations.mjs` to validate store schema version during startup.
`AGENT_TARGETS_SERVER` — maps agent targets to expected output file names, used to determine where to write the final context file.
`PRECISION_MODES` — imported from `ollama.mjs` to define valid precision modes for Ollama analysis.
`DEFAULT_RULES` — imported from `classifier.mjs` to define default file inclusion/exclusion rules.
`startupState` — global state object tracking migration progress and server readiness.
**Side effects:** 
Modifies global `appState` object with real-time run status, logs, results, and file statuses.
Writes to disk: `repodna-debug.log`, project cache files (`hashCachePath`, `resultCachePath`, `newFilesCachePath`, `modelStatsCachePath`), `ollama_models.json`, and project config file.
Subscribes to HTTP requests and SSE clients, managing connection lifecycle.
Initiates `child_process.exec` calls to check Ollama status and fetch GitHub release info.
**Architecture:** 
Imports and initializes global state from `config.mjs`, `scanner.mjs`, `ollama.mjs`, `builder.mjs`, `classifier.mjs`, `cache.mjs`, `migrations.mjs`, and `store.mjs`.
Owns the main HTTP server instance and routes all API endpoints.
Manages `appState` and `sseClients` as global singletons for real-time communication.
Exports functions for project management, analysis orchestration, and cache operations.
**Watch out:** 
The `startupState` object must be fully initialized before any API endpoint is served to ensure migration status is correctly reported.
The `runnerAbortController` and `retryLock` must be reset properly between runs to avoid race conditions.
The `hashCacheFile` and `resultCacheFile` paths must be correctly constructed to avoid cache corruption.
The `AGENT_TARGETS_SERVER` mapping must be updated when new agent targets are added to maintain correct output file naming.
The `runAnalysis` function assumes `config.changesOnly` is boolean and that `config.projectPath` exists and is readable.
The `fileHash` function can return `null` if a file cannot be read, which may affect cache behavior.

---

### `src/store.mjs`
**Role:** Manages the schema version of the `~/.repodna` data store, ensuring migrations run once at startup to handle schema updates and maintain data compatibility. It handles store initialization, wiping, and validation of the migration chain.
**Key exports:** `dataDir` — returns the default data directory path; `readStoreSchema` — reads the current schema version from the store; `writeStoreSchema` — writes the schema version to the store; `migrateStore` — runs the migration chain once and returns migration details; `validateMigrationsChain` — checks the integrity of the migration steps; none
**Non-obvious deps:** none
**Watch out:** The `wipeStore` function deletes files individually to avoid Windows permission issues, and `migrateStore` mutates the store in place during migration steps. The `validateMigrationsChain` function must be called before any store operations to ensure migration integrity.

---

### `tests/store.test.mjs`
**Role:** Tests the store migration logic, cache I/O operations, and migration chain consistency. Validates that the system correctly handles legacy stores, maintains crash-safe cache operations, and ensures migration steps are properly ordered and compatible.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** The test suite creates and cleans up temporary directories using `tmpdir()` and `mkdirSync`, which may conflict with global state if run in parallel. The `recoverCacheFile` function modifies file system state by removing `.bak` files and ignoring `.tmp` files during crash recovery, which could affect tests relying on exact file system contents.

---

### `uninstall.mjs`
**Role:** Removes the local repoDNA data directory (`~/.repodna`) after confirming the AI assistant server is not running and optionally prompting the user for confirmation.
**Exports:** none

---

### `update.mjs`
**Role:** Manages repoDNA's version updates, including compatibility checks, data store migration analysis, and Git-based installation updates. It ensures the local codebase aligns with a target version while handling user prompts, stashing changes, and informing about data store implications.
**Key exports:** none
**Non-obvious deps:** `fs`, `path`, `child_process`, `readline` — used for Git operations, CLI interaction, and file system access; `fetch` — for GitHub API and raw file retrieval; `process.argv` — for command-line argument parsing; `process.stdout`, `process.stdin` — for user prompts and output control
**Side effects:** Writes to stdout and stderr; performs Git operations (fetch, checkout, stash); modifies global process exit codes; may alter local Git working tree state
**Architecture:** Imports `package.json` for version and schema data; directly imports `fetch`, `execSync`, `readFileSync`, `join`, `dirname`, `fileURLToPath`, `createInterface` from Node.js built-ins; owns the update flow logic and compatibility analysis; exports no functions, only runs as a script
**Watch out:** Must run in a Git repository with a clean working tree unless `--force` is used; assumes `node` and `git` are available in PATH; relies on GitHub API for version metadata; incompatible schema updates wipe the data store on first launch

## Configuration

### `ollama_models.json`
**Role:** Defines the curated catalog of Ollama-compatible models specifically for code analysis and documentation generation, organized by VRAM requirements and performance characteristics for repoDNA's use case.
**Key exports:** none
**Non-obvious deps:** none
**Side effects:** none
**Architecture:** This file is a static configuration resource consumed by the repoDNA setup flow and model selection UI; it does not initialize or own any global state or singletons.
**Watch out:** The `models` array must remain sorted by `min_vram_gb` ascending for local models, and `id` values must be unique across all entries to avoid conflicts in model resolution logic.

---

### `package.json`
**Role:** Defines the project's metadata, entry point, and available scripts for running, testing, and uninstalling the repoDNA tool.
**Exports:** none

## Documentation

### `README.md`
**Role:** Documents the repoDNA tool's purpose, setup process, workflow, supported agents and project types, analysis modes, and command-line interface for users and developers.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** none

## CSS Styles

### `docs/landing.css`
**Role:** Defines the visual styling and layout for the project's main landing page, including navigation, hero section, feature cards, and video player components. This file exclusively manages the presentation layer for user-facing content and UI elements.
**Key exports:** none
**Non-obvious deps:** none
**Side effects:** none
**Architecture:** Imports `shared/tokens.css` for design system variables. Does not import or initialize any global state or singletons. All exports are CSS class names used in HTML.
**Watch out:** none

---

### `docs/shared/tokens.css`
**Role:** Defines shared design tokens for color, typography, and spacing used across the application and landing page, ensuring consistent styling.
**Exports:** none

---

### `public/styles.css`
**Role:** Defines the visual styling and layout for the entire application's user interface, including the top navigation bar, wizard steps, form elements, cards, and responsive design for different screen sizes.
**Key exports:** none
**Non-obvious deps:** none
**Side effects:** none
**Architecture:** Imports `./shared/tokens.css` for CSS variables; initializes global theme variables (`--bg`, `--t1`, `--a`, etc.) used throughout the app; owns the layout structure for the main content area, topbar, and wizard pages.
**Watch out:** The file uses CSS custom properties (`var(--bg)`, `var(--t1)`) that must be defined in `tokens.css` for proper rendering; the `@keyframes` definitions for `pageIn`, `btn-attention`, and `badge-shimmer` are critical for animations; media queries depend on specific breakpoints for responsive behavior.

## Templates

### `docs/index.html`
**Role:** Renders the main landing page for repoDNA, presenting project overview, installation instructions, video demonstrations, and interactive setup guidance for users beginning their repoDNA session.
**Key exports:** none
**Non-obvious deps:** none
**Side effects:** none
**Architecture:** Imports `landing.css` and `shared/animation1-standalone.html` for styling and animation; initializes global state via inline JavaScript for version display, GitHub star count, and video player functionality; owns the `vpGoTo` and `vpPlay` functions used in video navigation.
**Watch out:** The `VIDEO_SERIES` array must be correctly populated with valid YouTube video IDs for the video player to function; the `copyInstall` function requires the `install-cmd` elements to exist and contain valid command strings.

---

### `public/index.html`
**Role:** [Manages the entire single-page application UI layout, including the setup wizard, model selection, file analysis interface, and update notifications. Controls the visual flow of the user through five distinct steps: Setup, Project, Files, Options, and Run.]
**Key exports:** [none]
**Non-obvious deps:** [none]
**Side effects:** [none]
**Architecture:** [Imports `app.mjs` for application logic and `shared/dna.js` for shared utilities. Initializes global state via `window._recheckOllama`, `window._showModelCatalog`, `window._closeModelPopup`, and `window._copyPopupCmd`. Owns the main DOM structure and event handlers for UI interactions.]
**Watch out:** [The `typewriter` function modifies the `brand` element's content dynamically. The `makeDna` function updates the SVG element with color based on the primary button's background. The `startup-overlay` and `startup-banner` elements are conditionally displayed during server upgrades and should not be dismissed manually.]

## Other Files

### `LICENSE`
**Role:** This file defines the MIT license terms for the repoDNA project and reserves the exclusive intellectual property rights to the "repoDNA" name, logo, and domain.
**Exports:** none

---
*Generated by [repoDNA](https://github.com/BogdanVasaiu/repodna)*
