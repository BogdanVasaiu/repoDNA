# CLAUDE.md
> Auto-generated by repoDNA · Smart Update · Model: `qwen3-coder:30b`
> Generated: 2026-05-15T07:36:10.317Z

---

## Project Overview

 repoDNA is a five-step wizard that runs in your browser.

01 — Ollama setup Checks whether Ollama is running and lists all locally installed models. If Ollama is not installed, the interface shows the exact install command for your platform (Windows, macOS, Linux). A curated model reference table is also available here, updated regularly, with models ranked by quality, speed, and VRAM requirements — so you can pick and pull the right one before starting.

02 — Add your projects Register one or more local project paths with an optional description. The description is passed to the AI as context before any file is read.

03 — Select what to scan An intelligent classifier examines your project structure — detects Node.js, Python, Rust, Go, and more — and auto-excludes folders like node_modules, build output, and binary files. Every decision is visible and overridable per file or per folder. Custom rules are saved per project.

04 — Configure the analysis Select the Ollama model from a dropdown that lists everything currently installed in your Ollama instance. Set the analysis depth (see precision modes below). Advanced options include:

Smart Update — only re-analyzes files that changed since the last run
File tree — optionally embeds an ASCII tree of the project in the output (included files only, or full project)
05 — Run A live dashboard shows each file being processed, its markdown result appearing in real time, and a running activity log. Failed files can be retried individually. Each result card can also be edited directly in the UI — changes are saved to the cache and the output file is rebuilt immediately from the cache, not from the project. This keeps manual edits safe: the algorithm never overwrites them by re-reading the project files.

---

## File Tree

<pre class="file-tree-block">├── docs/
│   ├── shared/
│   │   ├── assets/
│   │   │   ├── agents/
│   │   │   │   ├── Amp-icon.svg
│   │   │   │   ├── Claude-icon.svg
│   │   │   │   ├── Cline-icon.svg
│   │   │   │   ├── Codex-icon.svg
│   │   │   │   ├── Copilot-icon.svg
│   │   │   │   ├── Cursor-icon.svg
│   │   │   │   ├── Gemini-icon.svg
│   │   │   │   ├── Generic-icon.svg
│   │   │   │   ├── Openclaw-icon.svg
│   │   │   │   ├── Opencode-icon.svg
│   │   │   │   ├── RooCode-icon.svg
│   │   │   │   └── Windsurf-icon.svg
│   │   │   ├── steps/
│   │   │   │   ├── Screenshot 2026-05-14 102440.png
│   │   │   │   ├── step-01.png
│   │   │   │   ├── step-02.png
│   │   │   │   ├── step-03.png
│   │   │   │   ├── step-04.png
│   │   │   │   └── step-05.png
│   │   │   ├── nodejs-icon.svg
│   │   │   └── ollama-icon.svg
│   │   ├── animation1-standalone.html
│   │   ├── dna.js
│   │   └── tokens.css
│   ├── CNAME
│   ├── index.html
│   └── landing.css
├── public/
│   ├── shared/
│   │   ├── assets/
│   │   │   ├── agents/
│   │   │   │   ├── Amp-icon.svg
│   │   │   │   ├── Claude-icon.svg
│   │   │   │   ├── Cline-icon.svg
│   │   │   │   ├── Codex-icon.svg
│   │   │   │   ├── Copilot-icon.svg
│   │   │   │   ├── Cursor-icon.svg
│   │   │   │   ├── Gemini-icon.svg
│   │   │   │   ├── Generic-icon.svg
│   │   │   │   ├── Openclaw-icon.svg
│   │   │   │   ├── Opencode-icon.svg
│   │   │   │   ├── RooCode-icon.svg
│   │   │   │   └── Windsurf-icon.svg
│   │   │   ├── nodejs-icon.svg
│   │   │   └── ollama-icon.svg
│   │   ├── animation1-standalone.html
│   │   ├── dna.js
│   │   └── tokens.css
│   ├── app.mjs
│   ├── index.html
│   └── styles.css
├── builder.mjs
├── classifier.mjs
├── CLAUDE.md
├── config.mjs
├── debug.mjs
├── LICENSE
├── main.mjs
├── ollama_models.json
├── ollama.mjs
├── package.json
├── README.md
├── scanner.mjs
├── server.mjs
└── uninstall.mjs</pre>

---

## Dependency Graph

> Auto-extracted from static import analysis.

**`server.mjs`** imports: `./config.mjs`, `./scanner.mjs`, `./ollama.mjs`, `./builder.mjs`, `./classifier.mjs`
**`main.mjs`** imports: `./server.mjs`
**`scanner.mjs`** imports: `./classifier.mjs`

### Most imported modules
- `./classifier.mjs` ← used by 2 files
- `./server.mjs` ← used by 1 file
- `./config.mjs` ← used by 1 file
- `./scanner.mjs` ← used by 1 file
- `./ollama.mjs` ← used by 1 file
- `./builder.mjs` ← used by 1 file

---
 
## Styles & CSS

### `docs\landing.css`
**Role:** Defines the visual styling and layout for the main landing page, including typography, color schemes, interactive elements, and responsive design for various screen sizes.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** The file uses CSS custom properties (variables) imported from `shared/tokens.css` which must be present for styles to render correctly; global state mutations are not present, but the file relies on CSS variable definitions that affect the entire document.

---

### `docs\shared\tokens.css`
**Role:** Defines shared design tokens for the repoDNA application and landing page, including color palettes, typography, and spacing values used across the UI.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** none

---

### `public\shared\tokens.css`
**Role:** Defines shared design tokens for the repoDNA application and landing page, including colors, typography, and spacing values used throughout the UI.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** none

---

### `public\styles.css`
**Role:** Defines the visual styling and layout for the repoDNA wizard interface, including app structure, form elements, navigation buttons, project list items, and model selection components.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** The file uses CSS custom properties (variables) from `./shared/tokens.css` and relies on specific class names and DOM structure for interactive elements like step pills and model dropdowns to function correctly.

## Utilities & Helpers

### `docs\shared\dna.js`
**Role:** Generates animated DNA helix and typewriter effects for the repoDNA project's branding and user interface.
**Key exports:** `makeDna` — creates an animated DNA helix SVG visualization; `typewriter` — animates the repoDNA brand text with a typewriter effect.
**Non-obvious deps:** none
**Watch out:** The `makeDna` function mutates the DOM by directly appending and removing SVG elements, and relies on global `requestAnimationFrame` for animation loop. The `typewriter` function manipulates innerHTML and uses setTimeout for timing, which may conflict with other DOM updates if not isolated.

---

### `public\shared\dna.js`
**Role:** Renders an animated DNA helix SVG and a typewriter effect for the repoDNA brand text.
**Key exports:** `makeDna` — creates and animates a DNA helix SVG element using SVG DOM methods; `typewriter` — animates the display of the "repoDNA" text with a typewriter effect.
**Non-obvious deps:** none
**Watch out:** The `makeDna` function mutates the DOM directly by appending and removing SVG elements in a loop; `typewriter` modifies the innerHTML of a target element and relies on CSS classes for styling.

## Other Files

### `builder.mjs`
**Role:** Categorizes project files into predefined UI component, service, utility, and other groups; builds a dependency graph from static import analysis; generates an ASCII file tree; and constructs the final markdown output for the AI analysis report.
**Key exports:** `categorize` — maps a file path to a category label based on its location or naming conventions; `buildDependencyGraph` — constructs a map of file imports using static analysis; `buildAsciiTreeMd` — generates an ASCII tree representation of a list of file paths; `buildClaudeMd` — assembles the complete markdown report including project overview, dependency graph, file tree, and categorized file summaries; `CATEGORY_META` — defines the mapping of category labels to UI labels and titles; `buildHeader` — generates the markdown header with agent, model, and precision information; `parseImports` — extracts import statements from source files using regex patterns; `formatDependencyGraph` — formats the dependency graph into a markdown table.
**Non-obvious deps:** none
**Watch out:** The `buildDependencyGraph` function mutates a `Set` to collect imports, and `buildAsciiTreeMd` uses recursive rendering which may cause stack overflow with deeply nested paths. The `buildClaudeMd` function relies on `CATEGORY_META` ordering for section generation and assumes `results` keys match the category names. The `categorize` function uses regex patterns that are not validated for correctness at runtime.

---

### `classifier.mjs`
**Role:** Determines which files and folders to include or exclude during project analysis based on project type, file extensions, filenames, and size. It applies a priority-based system to make decisions and supports user overrides.
**Key exports:**
 `DEFAULT_EXCLUDED_FOLDERS` — an array of folder names to always skip during analysis  
`DEFAULT_EXCLUDED_FILENAMES` — an array of filenames to always skip during analysis  
`DEFAULT_EXCLUDED_EXTENSIONS` — an array of file extensions to always skip during analysis  
`DEFAULT_INCLUDED_FILENAMES` — an array of filenames that are always included regardless of extension  
`DEFAULT_INCLUDED_EXTENSIONS` — an array of file extensions that are always included regardless of filename  
`detectProjectType` — identifies the project type (e.g., NODE_JS, PYTHON) based on root-level files  
`getAutoStatus` — calculates whether a file should be included, excluded, or marked as ambiguous based on rules  
`computeFinalStatus` — returns the final status of a file after applying user overrides  
*...and 1 more (file too large to list all)*
**Non-obvious deps:** none
**Watch out:** The `PROJECT_RULES` object is a global constant that defines project-specific overrides; changes affect how different project types are classified. The `getAutoStatus` function relies on a strict priority order, and modifying the priority logic may alter behavior significantly.

---

### `config.mjs`
**Role:** Manages global and per-project configuration data for repoDNA, including project registration, settings persistence, and cache file paths. Handles migration of legacy config formats and ensures consistent data storage in `~/.repodna` with project-specific subdirectories.
**Key exports:**
`loadConfig` — reads and returns the global configuration object, migrating legacy formats if needed
`saveConfig` — writes the provided configuration object to `~/.repodna/config.json`
`getProjectList` — returns the list of registered projects from the global config
`addProject` — registers a new project or updates an existing one with default settings and custom rules
`removeProject` — unregisters a project and deletes its associated cache files in `~/.repodna/projects/<encoded-path>/`
`updateProjectSettings` — updates specific settings for a registered project
`getProject` — retrieves a project entry by its path from the global config
`hashCachePath` — returns the path to the hashes cache file for a given project
*...and 2 more (file too large to list all)*
**Non-obvious deps:** none
**Watch out:** The `loadConfig` function performs a one-time migration of v1 config format to v2, which modifies the global config object in memory before returning it. The `addProject` function mutates the global config object directly and saves it to disk, so it should not be called concurrently.

---

### `debug.mjs`
**Role:** Enables and manages debug mode for repoDNA, including logging file processing details to a dedicated log file.
**Key exports:** `isDebugEnabled` — checks if debug mode is enabled by verifying the existence of a flag file. `enableDebug` — creates a flag file to activate debug mode. `disableDebug` — removes the flag file to disable debug mode. `clearDebugLog` — initializes or resets the debug log file with a header. `debugLog` — appends a formatted table row and JSON entry to the debug log file when debug mode is active.
**Non-obvious deps:** none
**Watch out:** The debug log file is overwritten on each run when `clearDebugLog` is called, and the flag file determines whether logging occurs. Modifying the log file path or flag file name would break debug functionality.

---

### `docs\index.html`
**Role:** Serves as the main landing page for the repoDNA project, presenting project overview, installation instructions, feature highlights, and video demonstrations to users.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** The page dynamically loads content via JavaScript (e.g., version info, star count) and injects YouTube iframes on video play, which may affect rendering timing and requires network access for external resources.

---

### `LICENSE`
**Role:** Defines the legal terms under which the repoDNA software may be used, modified, and distributed, while also protecting the intellectual property rights associated with the project's brand identity.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** none

---

### `main.mjs`
**Role:** Entry point for the repoDNA application that initializes the server and handles command-line arguments, including enabling debug mode.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** none

---

### `ollama.mjs`
**Role:** Configures and executes AI analysis of project files using Ollama, with adaptive precision modes, smart truncation of large files, and fallback to OpenAI-compatible endpoints.
**Key exports:** `PRECISION_MODES` — defines fast, standard, deep, and adaptive analysis modes with token and character limits, temperature settings, and descriptions  
`checkOllama` — verifies Ollama availability and lists installed models  
`getEffectiveMode` — resolves adaptive precision mode to fast/standard/deep based on file size  
`smartTruncate` — preserves import blocks and shows head/tail of large files for context  
`buildPromptForTier` — generates structured prompts for AI analysis based on analysis depth  
`analyzeFileWithOllama` — orchestrates file reading, prompt building, and Ollama API calls with fallbacks  
`stripThinking` — removes `<think>` blocks from AI responses for cleaner output  
`isThinkingModel` — identifies models that output in a "thinking" format requiring post-processing  
none
**Non-obvious deps:** none
**Watch out:** The `getEffectiveMode` function mutates the returned mode object with a `_name` property; `smartTruncate` modifies the input content by slicing and joining lines; `analyzeFileWithOllama` may throw `AbortError` if the signal is aborted, and the `postProcess` function modifies AI output by stripping thinking blocks and normalizing list formatting.

---

### `ollama_models.json`
**Role:** Provides a curated, ranked catalog of Ollama-compatible models optimized for code analysis and documentation generation, including local and cloud options with detailed metadata for VRAM requirements, context windows, and performance scores.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** none

---

### `package.json`
**Role:** Defines the project's metadata, entry point, and executable scripts for starting the application and uninstalling it.
**Key exports:** `main` — specifies the primary entry file (`main.mjs`) that runs the application  
**Non-obvious deps:** none  
**Watch out:** none

---

### `public\app.mjs`
**Role:** Manages global application state, UI rendering, and project configuration for the repoDNA wizard; handles markdown processing, file tree visualization, and real-time result updates during code analysis.
**Key exports:** `S` — holds all global state including current page, Ollama settings, project paths, file statuses, and analysis parameters; `md2html` — converts markdown text to HTML with fallback handling; `applyProjectSettings` — restores saved project configuration from storage; `_buildProjectPayload` — constructs JSON payload for saving project settings; `getFileBadge` — generates HTML file type badges with color coding; `getFolderColor` — returns color for folder icons based on common naming conventions; `renderPreview` — displays AI output preview panel with copy functionality; `saveCurrentProjectSettings` — debounced API call to persist current project settings.
**Non-obvious deps:** none
**Watch out:** The file mutates global state via the `S` object and relies on DOM elements being present for UI updates. It initializes UI components on `DOMContentLoaded` and may race with server events if not all state is loaded before rendering. The `S.results` object is modified directly during result edits, which affects the cached output and must be synchronized with the server.

---

### `public\index.html`
**Role:** [The entry point for the repoDNA web application that renders the multi-step wizard interface for setting up Ollama, adding projects, selecting files, configuring analysis options, and running AI-powered code analysis. It initializes the UI components, manages the application's visual state through JavaScript, and loads external dependencies like Markdown rendering and CSS styles.]
**Key exports:** [none]
**Non-obvious deps:** [marked 11.1.0 — used for rendering Markdown in the UI; external CSS file `styles.css` that defines UI layout and theming; `app.mjs` module that handles core application logic; `shared/dna.js` that provides DNA icon generation and typewriter effects; `window._recheckOllama`, `window._selectOs`, `window._resumeRun`, `window._stopRun`, `window._exitToStart`, `window._closeModelPopup`, `window._copyPopupCmd` — global functions attached to the window object for UI interaction handlers]
**Watch out:** [The HTML structure relies on specific IDs and class names used by JavaScript for DOM manipulation and state updates; global functions like `window._recheckOllama` and `window._selectOs` must be defined elsewhere in the codebase; the typewriter effect and DNA icon rendering depend on `typewriter()` and `makeDna()` functions from `shared/dna.js` which are called directly in the script block; the update banner and model pull popup are controlled via inline event handlers and global functions, not component state]

---

### `README.md`
**Role:** Documents the repoDNA project setup, usage instructions, analysis modes, output formats, and data storage details for users configuring and running the local AI context generator.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** none

---

### `scanner.mjs`
**Role:** Scans a project directory structure, classifies files and folders based on project type and rules, and builds a hierarchical tree of nodes representing the project's structure. It also provides a function to count file extensions within a project for analysis purposes.
**Key exports:** 
`scanProject` — scans a project directory and returns a tree structure of nodes, file count, and project type, with classification and exclusion logic applied.
`scanProjectExtensions` — analyzes a project directory and returns a sorted list of file extensions and their counts, excluding common build and cache directories.
**Non-obvious deps:** none
**Watch out:** The `scanProject` function mutates node objects in-place during the classification and status computation steps, and relies on the `computeFinalStatus` function from `classifier.mjs` to determine final inclusion status. The `buildTree` helper function modifies node objects in the map during tree construction.

---

### `server.mjs`
**Role:** Serves the repoDNA web interface, manages application state for the analysis wizard, handles file scanning and AI analysis workflows, and provides API endpoints for client communication and configuration management.
**Key exports:** none
**Non-obvious deps:** `config.mjs` — consumed for project and cache paths; `scanner.mjs` — used for project scanning logic; `ollama.mjs` — used for Ollama model checks and file analysis; `builder.mjs` — used for markdown output generation; `classifier.mjs` — used for default rule loading
**Watch out:** The `appState` global object is mutated throughout the file and shared across multiple request handlers; `runnerAbortController` is reset on each run and can be cancelled via `abort()`; `retryQueue` and `retryRunning` manage serial retry logic and must be handled carefully to prevent race conditions; `catResultMaps` uses Maps per category for deduplication and is reset on each new run; `sseClients` array is modified by `push()` and `push()` itself can silently fail on client write errors

---

### `uninstall.mjs`
**Role:** Removes the repoDNA data directory (`~/.repodna`) from the user's home folder, including all cached analysis results and configuration data, while leaving project files untouched.
**Key exports:** none
**Non-obvious deps:** none
**Watch out:** none

---
*Generated by [repoDNA](https://github.com/BogdanVasaiu/repodna)*
